<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace= "com.nh.book.book.model.mapper.BookMapper">

<select id="selectBookList" resultType = "Book">
SELECT BOOK_NO, BOOK_TITLE, BOOK_WRITER, BOOK_PRICE, TO_CHAR(REG_DATE,'YYYY-MM-DD HH24:MI:SS') REG_DATE 
FROM "BOOK"
</select>
 
</mapper>
